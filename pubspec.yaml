name: ai_secure_access
description: "AI Secure Access – Face Recognition & Security Overlay System"
publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: "^3.3.0"   # Flutter 3.22 uses Dart 3.3

dependencies:
  flutter:
    sdk: flutter

  # 🖥️ UI & Fonts
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # 🎥 Camera & Face Detection
  camera: ^0.11.3
  google_mlkit_face_detection: ^0.13.1

  # 🧠 Face Recognition (FaceNet - TFLite)
  tflite_flutter: ^0.11.0

  # 🖼 Image Processing (REQUIRED for FaceNet)
  image: ^4.1.7

  # ⚙ Utilities
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  http: ^1.2.2
  intl: ^0.20.1

  # 🔐 Encryption
  encrypt: ^5.0.3

  # 📁 File Picker
  file_picker: ^10.3.7

  # 🌐 URL Launcher
  url_launcher: ^6.2.5

  # 📧 Email Sending
  mailer: ^6.1.0

  # 🪟 Overlay Window
  flutter_overlay_window: ^0.5.0

  # 🧠 App Icons
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icon/
    - assets/models/facenet (2).tflite   # ✅ FIXED (no spaces, no brackets)

# ⚙️ Launcher Icon Configuration
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#1F2C3A"
  adaptive_icon_foreground: "assets/icon/app_icon.png"
